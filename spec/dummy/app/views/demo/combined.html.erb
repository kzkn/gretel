<% breadcrumb :combined %>

<div class="demo-section">
  <h2>Combined: Arrow Separator + Large Size</h2>

  <div class="code">
    &lt;%= breadcrumbs style: :bulma, class: "breadcrumb has-arrow-separator is-large" %&gt;
  </div>

  <div class="result">
    <%= breadcrumbs style: :bulma, class: "breadcrumb has-arrow-separator is-large" %>
  </div>

  <div class="content">
    <h3>Description:</h3>
    <p>
      Bulma breadcrumb combining arrow separator with large size. Uses both
      <code>has-arrow-separator</code> and <code>is-large</code> classes together.
    </p>

    <p>
      You can combine any separator with any size to create different breadcrumb styles.
    </p>
  </div>
</div>
