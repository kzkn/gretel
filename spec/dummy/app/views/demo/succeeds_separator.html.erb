<% breadcrumb :succeeds_separator %>

<div class="demo-section">
  <h2>Succeeds Separator (≻)</h2>

  <div class="code">
    &lt;%= breadcrumbs style: :bulma, class: "breadcrumb has-succeeds-separator" %&gt;
  </div>

  <div class="result">
    <%= breadcrumbs style: :bulma, class: "breadcrumb has-succeeds-separator" %>
  </div>

  <div class="content">
    <h3>Description:</h3>
    <p>
      Bulma breadcrumb with succeeds separator. Uses the <code>has-succeeds-separator</code> class
      to display a succeeds symbol (≻) between breadcrumb items.
    </p>
  </div>
</div>
